import React from 'react';
import {View} from 'react-native';
import {NavigationProps} from '../navigation/types';

const Blog: React.FC<NavigationProps> = ({navigation, route}) => {
  return <View />;
};

export default Blog;
