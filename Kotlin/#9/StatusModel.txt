package com.example.oneentry.model

data class StatusModel(
    val id: Int,
    val identifier: String,
    val localizeInfos: Map<String, StatusTitle>
)

data class StatusTitle(
    val title: String
)

enum class IdentifierStatus {
    inStock, outStock
}